# Worldmonitor Research Swarm Synthesis, 2026W28

## What The Data Says

Measured on 2026-07-07 from
`/home/hermes/worktrees/worldmonitor/t_a9338e89`, branch
`codex/worldmonitor/t_a9338e89-synthesize-the-research-swarm-for-worldmonitor-2`.

The committed `research/dossier/` is now canonical for this app repo. The Loop
dossier at `/home/hermes/Loop/research/dossiers/worldmonitor.md` is consistent
with it on the north-star and constraints, but the repo note is newer than the
seed. This pass reconciles the repo note, the three parent lens handoffs, and
fresh read-only probes.

Prior recommendation retro: PR #5,
`docs: synthesize worldmonitor research swarm`, merged on 2026-07-07 and
created the first living dossier. Its selected implementation change was a
compact data-health rollup in `/api/service-status`. That recommendation
shipped as documentation only; no implementation PR is open, and the target
metric has not moved. Current local probes still show green service-status
alongside unhealthy compact data readiness.

Open PR overlap check:
`gh pr list --repo TheSacud/worldmonitor --state open --json number,title,headRefName,body`
returned `[]` on 2026-07-07, so no open PR already ships or proposes the
selected next change.

Current success and diagnostic metrics:

| Metric | Current value | Interpretation |
| --- | --- | --- |
| Origin exposure | TCP to `91.99.197.46:3010` was `closed_or_filtered`; TCP to `127.0.0.1:3010` connected; `ss -H -ltn` shows `127.0.0.1:3010` | Closed |
| Local service status | `GET http://127.0.0.1:3010/api/service-status` returned HTTP 200, `success:true`, timestamp `2026-07-07T23:01:52.000Z`, and 2 operational services | Green, but shallow |
| Compact data health | `GET http://127.0.0.1:3010/api/health?compact=1` returned HTTP 200 with `status:"UNHEALTHY"` and summary `total:196 ok:93 warn:21 onDemandWarn:21 crit:61` | Degraded and not reflected in service-status |
| Core UI | `GET http://127.0.0.1:3010/` returned HTTP 200 `text/html` | Serving locally |
| Docker health | Not remeasured because Docker is prohibited in this worker lane; Loop signal says `worldmonitor` became healthy after the 2026-07-05 healthcheck remediation | Measurement gap |
| AIS relay health | Not remeasured because Docker is prohibited in this worker lane; Loop signal says `worldmonitor-ais-relay` became healthy after relay secret pass-through | Measurement gap |
| Public HTTPS path | Approved helper fetched `https://wm.sacud.com/` and `https://wm.sacud.com/api/service-status` on 2026-07-07; both returned HTTP 200 Cloudflare Access login pages | Public edge is reachable, app body remains auth-gated from this lane |
| Protected dependency surface | Unauthenticated maritime vessel snapshot returned HTTP 401 with `cache-control: no-store` | No unauthenticated AIS payload exposed |

## Parent Lens Inputs

Ingress security found the original public Docker app-port exposure closed.
The current refresh agrees: app-facing `3010` is loopback-only from listener and
TCP probes, and the Loop signal records the 2026-07-05 remediation. The best
remaining ingress work is regression prevention: compose port-binding tests,
non-secret audit output, and a sanctioned public-path smoke that distinguishes
Cloudflare Access from authenticated app reachability.

Runtime health found that `/api/service-status` and compact health can disagree.
The disagreement is still live and slightly worse than the prior committed
note: compact critical checks are now 61, while service-status still reports
two operational services. Docker health could not be remeasured without Docker,
so the Loop signal remains the only allowed Docker-health evidence here.

Dependency degradation found that protected data surfaces degrade without
obvious unauthenticated leakage, but Redis limiter compatibility remains open.
The Loop signal records `Command not allowed: EVAL`, and source verification
shows `api/_rate-limit.js` still uses `@upstash/ratelimit` sliding windows.
The AIS relay is healthy when configured according to the Loop signal, but
source still exits when `AISSTREAM_API_KEY` is absent.

No parent lens was missing a `## LENS FINDINGS` handoff. The shared unknowns
are Docker health, Docker NAT rules, and live Redis error frequency, all of
which require prohibited Docker, broad sudo, logs, or private runtime state.

## Prior Recommendation Retro

The previous top pick was the service-status compact-health rollup. It did not
ship as code; PR #5 only added the living dossier and W27 synthesis note. The
target metric did not move: service-status still returns HTTP 200 with
`success:true` and all services operational, while compact health is still
`UNHEALTHY` with 61 critical checks.

The earlier seed top pick, rebinding the worldmonitor Docker port to loopback,
remains shipped operationally. Its target metric has moved: TCP to
`91.99.197.46:3010` is closed or filtered, local `127.0.0.1:3010` connects,
and compose binds `127.0.0.1:${WM_PORT:-3000}:8080`.

The seed Docker-health and AIS relay health items are reported closed by the
Loop signal after the healthcheck override and relay secret pass-through. They
were not directly remeasured here because this lane cannot run Docker.

## Merged Scored Roadmap

| Score | Status | Milestone | Lenses | Target metric |
| --- | --- | --- | --- | --- |
| 14.1 | Selected next, carried forward | Add compact data-health rollup to `/api/service-status`, preserving HTTP 200 liveness while marking aggregate dependency state degraded or unhealthy when compact health is unhealthy | Runtime health, Dependency degradation | Authenticated degradation observability and local health fidelity |
| 9.2 | Open | Replace the `@upstash/ratelimit` Lua-backed limiter path, or make it REST-command-compatible, so deployed Redis REST no longer denies limiter commands | Dependency degradation | Rate-limit degradation behavior |
| 8.5 | Open | Add Redis REST command-parity self-check for limiter Lua commands and expose only aggregate pass/fail in health | Dependency degradation | Rate-limit degradation observability |
| 8.5 | Open | Improve Docker health fidelity by checking the same nginx-served path users depend on plus one safe internal API reachability check | Runtime health | Docker health fidelity |
| 7.1 | Guardrail | Add a CI compose test that rejects app-facing non-loopback host port binds unless explicitly allowlisted | Ingress security | Origin exposure stays closed |
| 7.1 | Guardrail | Add a no-auth/degraded smoke contract for protected dependency surfaces | Dependency degradation | Safe authenticated degradation |
| 7.1 | Open | Make operator status taxonomy explicit: runtime liveness, data readiness, public ingress, and dependency degradation | Runtime health | Alert accuracy |
| 6.9 | Open | Add a browser-session-safe bootstrap availability probe without session minting or payload dumps | Runtime health | Core UI hydration health |
| 6.9 | Open | Surface compact-health dependency categories in UI/status surfaces without exposing unauthenticated internals | Dependency degradation | Safe visible degradation |
| 6.9 | Open | Decide AIS behavior when `AISSTREAM_API_KEY` is absent or keep env pass-through as a required deployment invariant | Dependency degradation | AIS relay health |
| 6.0 | Partial | Add a sanctioned public-path smoke that distinguishes Cloudflare Access challenge, Nginx reachability, and authenticated app body reachability without exposing credentials | Ingress security | Public ingress validation coverage |
| 5.7 | Backlog | Add a non-secret ingress audit runbook or script | Ingress security | Origin exposure detection |
| 4.6 | Backlog | Triage compact-health critical checks into seed-bundle and API-owner buckets | Dependency degradation | Local health critical count |

Conflict resolution: ingress work no longer outranks runtime/dependency work
because the direct origin exposure metric is closed. Runtime health and
dependency degradation continue to converge on one milestone: the app needs a
safe aggregate status surface that reports dependency/data readiness without
turning Docker liveness into a flapping non-200 check. Redis limiter
compatibility is the strongest next dependency fix, but it is second because
operators still cannot see aggregate dependency failure from service-status.

## Selected Next Change

Selected next implementation change: carry forward the compact data-health
rollup in `/api/service-status`.

Implementation intent:

- Keep `/api/service-status` unauthenticated and HTTP 200 for liveness and
  Docker healthcheck compatibility.
- Add only aggregate data-readiness fields derived from compact health, such as
  status and summary counts, with no provider payloads, secrets, key names, or
  logs.
- Mark aggregate service-status state degraded or unhealthy when compact health
  is unhealthy, and mark data-readiness unknown if the compact health check
  itself fails.
- Preserve existing auth behavior for detailed health and API routes.

How to test it:

- Add focused tests in `src-tauri/sidecar/local-api-server.test.mjs` for
  healthy compact health, unhealthy compact health, and compact-health fetch
  failure.
- Run `node --test src-tauri/sidecar/local-api-server.test.mjs`.
- Run `node --test tests/health-redis-down-status.test.mjs`.
- In a local or staging self-host run, compare
  `GET http://127.0.0.1:3010/api/service-status` with
  `GET http://127.0.0.1:3010/api/health?compact=1`; service-status should stay
  HTTP 200 while exposing only aggregate data-readiness state.

## Validation Gaps

- Docker health, Docker published bindings, and Docker NAT rules were not
  remeasured because this worker is prohibited from using Docker or broad sudo.
- Live Redis error frequency was not measured because logs, Redis state, and
  private runtime state are out of scope.
- Public `wm.sacud.com` app-body behavior after Cloudflare Access authentication
  was not tested because this worker must not use cookies, credentials, or auth
  headers.
- No secrets, `.env` files, databases, logs, Docker inspect output, or private
  runtime state were read.
